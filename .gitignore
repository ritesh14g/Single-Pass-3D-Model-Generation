.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# run data — never commit payloads
data/raw/*
data/interim/*
data/outputs/*
# results brought back from the GPU box (scripts/box_collect.sh)
data/box/
# bundles packed for the GPU box (scripts/box_pack.py)
data/box_upload/
# terrain tiles fetched by the altitude-datum check (src/geo/vertical.py)
data/cache/
# independent reference surfaces (lidar, surveys) for Stage 6 accuracy
data/reference/
!data/**/.gitkeep

# model weights
weights/
*.pth
*.pt
*.onnx

*.log
.DS_Store

# Stage Lab runs, synthetic inputs, uploads, history
data/lab/

# third-party binaries (OpenMVS etc.), fetched per machine
tools/
