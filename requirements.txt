# Python 3.10. Install into a venv:
#   python -m venv .venv --system-site-packages
#   .venv\Scripts\python -m pip install -r requirements.txt
#
# Core (Stages 1-2, tests)
numpy>=2.0
scipy>=1.11
pandas>=2.0
pyarrow>=15
opencv-python>=4.9
pyyaml>=6
click>=8
tqdm>=4
pytest>=8

# Stage Lab UI (streamlit run ui/app.py)
streamlit>=1.40
altair>=5

# Geospatial / export (Stage 5 — installed, not yet used)
pyproj>=3.6
laspy>=2.5
rasterio>=1.3
trimesh>=4
pygltflib>=1.16

av>=12        # PyAV: container/codec probing and keyframe sampling (Stage 0), I-frame detection (§5.3)

# Optional — each has a logged fallback when absent
# pyulog        # PX4 .ulg flight logs (Stage 0/1 telemetry)
# pymavlink     # ArduPilot .bin / MAVLink .tlog logs (Stage 0/1 telemetry)
# ultralytics   # YOLOv8-seg: semantic dynamic-object masking (§5.5)
# torch         # Track B (§7.2); CUDA build on the GPU box
# PyNvVideoCodec  # NVDEC GPU decode (§4.1); needs torch with CUDA. Falls back to OpenCV

# Stage 4 Track A (§7.1): COLMAP SfM/MVS. Install ONE of these, never both (same module name):
#   laptop / no GPU:  pycolmap==4.2.0          (CPU wheel; Windows, Linux, macOS)
#   GPU box (Linux):  pycolmap-cuda12==4.2.0   (CUDA SIFT, matching, BA, PatchMatch stereo)
# OpenMVS 2.4.0 binaries (texturing; CPU dense fallback) go in tools/openmvs/, not pip.
plyfile>=1.0   # Track A reads dense PLYs (OpenMVS clouds carry per-point view lists)

# Stage 5 FBX (§8.3): portable Blender 4.2 LTS unpacked into tools/ (not pip):
#   https://download.blender.org/release/Blender4.2/  (blender-4.2.3-linux-x64.tar.xz / -windows-x64.zip)
